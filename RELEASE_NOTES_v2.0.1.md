# TeacherPro v2.0.1

Patch release with one bug fix.

## Fixed
- Renaming a lesson (via the subject metadata field) no longer moves the file out of its subfolder. If a lesson lives in e.g. `Lesson Plans/History/`, changing the subject now renames it within that same folder instead of dropping it back to the root of `Lesson Plans/`.
